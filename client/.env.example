# Leave empty so the browser talks to the same HTTPS origin (Vite proxy).
VITE_SERVER_URL=

# Where Vite forwards API + Socket.IO (host machine)
VITE_API_PROXY=http://127.0.0.1:3002

VITE_AUTH_TOKEN=
